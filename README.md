# Screen Name Generator

Generates screen names from two random 5-letter words (optionally with separator and digits).

## Setup
1) Build the 5-letter word list:
   python3 scripts/build_words5.py

2) Run locally:
   python3 -m http.server 8000 --directory src

Open: http://localhost:8000

